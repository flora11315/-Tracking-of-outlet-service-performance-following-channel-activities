services:
  - type: web
    name: channel-activity-demo-workbench
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python demo_workbench.py --host 0.0.0.0 --port $PORT
    autoDeploy: true
